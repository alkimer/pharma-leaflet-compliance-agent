# ============================================================================
# Web interface dependencies (`python run_web.py`).
#
# Only needed for the web UI: the terminal pipeline (`run_pipeline.py`) does not
# require any of them.
#
#   pip install -r requirements-web.txt
# ============================================================================

fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Required by the form's <input type="file">
python-multipart>=0.0.9
